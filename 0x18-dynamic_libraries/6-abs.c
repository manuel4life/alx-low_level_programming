#include "main.h"

/**
 * _abs - function that computes the value of an integer
 * @a: integer input
 * Return: absolute value
 */

int _abs(int a)
{
	if (a > 0 || a == 0)
	{
	return (a);
	}
	else
	return (a * -1);
}#include "main.h"

/**
 * _abs - function that computes the value of an integer
 * @a: integer input
 * Return: absolute value
 */

int _abs(int a)
{
	if (a > 0 || a == 0)
	{
	return (a);
	}
	else
	return (a * -1);
}
