#include "main.h"

/**
 * factorial - a function that returns the factorial of a given number
 * @n: integer input
 * Return: factorial
 */

int factorial(int n)
{
	int i;

	if (n < i)
	{
		return (1);
	}
	return (n * factorial(n - 1));
}
