#include "main.h"

/**
 * _abs - function that computes the value of an integer
 * @int: integer input
 * Return: absolute value
 */

int _abs(int)
{
	if (a > 0 || a == 0)
	{
	return (a);
	}
	else
	return (a * -1);
}
